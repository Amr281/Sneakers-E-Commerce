

export interface Product {
    id: number;
    // color:string;
    name: string;
    description: string;
    price: number;
    category: string;
    image: string;
    quantity?: number;  // Optional field for quantity
  }
  